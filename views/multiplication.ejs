<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Multiplicação e Soma</title>
        <link rel="stylesheet" href="styles.css">
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;400;700;900&display=swap" rel="stylesheet">
    </head>

    <body>
        <div class="container">
            <div class="form-style">
                <form action="/multiplicationResult" method="POST">
                    <h1>Multiplicação</h1>
                    <p class="values"><%= firstValue = Math.round(Math.random() * 1000) %></p>
                    <p class="sum">x</p>
                    <p class="values"><%= secondValue = Math.round(Math.random() * 100) %></p>
                    <input type="text" name="result" placeholder="resultado">
                    <button type="submit">Enviar</button>
                    <a href="/">Clique aqui para ir para a Soma</a>
                </form>
            </div>
        </div>
    </body>

</html>